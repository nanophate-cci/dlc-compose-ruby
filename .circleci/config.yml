
version: 2.1

jobs:
  build:
    machine:
      image: ubuntu-2204:current
      docker_layer_caching: true
    resource_class: medium
    steps:
      - checkout
      - run: docker images -a
      - run: docker-compose run ruby_script --verbose
      - run: docker images -a
